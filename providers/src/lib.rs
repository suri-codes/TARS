mod recurring;
pub use recurring::*;

pub trait Provider {
    parse_from_config()
    // need to parse out a config
    //
    // duration / how often they should repeat
    //
    // the actual task they need to run that could return a task
    //
    // what group they are linked to?
}
